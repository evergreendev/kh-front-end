import {Banner, Footer, Hour, Navigation, SiteOption} from "@/app/types/payloadTypes";

export type Meta = {
    siteOptions: SiteOption,
    hours: Hour,
    footer: Footer,
    nav: Navigation,
    banner: Banner,
}
